# this is a simple guess-game
# user have to simply guess a number between: 
# 1 and 20 and based-upon that computer will verify whether is is same or not as of random-number generated by computer
# import random
n=20
guess=0
guess_num=int(n*random.random())+1
while guess != guess_num:
    guess=int(input("new no : "))
    if guess>0 :
        if guess>guess_num:
            print("number is too large")
        elif guess<guess_num:
            print("number is too small")
    else:
            print("sorry ! but you are giving up")
            break;
else:
        print("congrats ! you guessed write")
